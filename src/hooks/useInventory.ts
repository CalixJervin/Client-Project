export { useInventory } from '../context/InventoryContext';
export type { 
  Ingredient, Recipe, Product, Sale, 
  RestockEntry, RecipeIngredient, ProductVariant,
  IngredientStatus 
} from '../types/inventory';
